Bolt Demo (RGB stream and joints)

In old Unity version need to download via package manager

When creating this demo through the were added ("Tools -> Bolt -> Unit Option Wizard"):

Assembly:

- nuitrack.net

Types:

- Users
- User Data									(UserData)
- Skeleton Data of User Data				(UserData.SkeletonData)
- Joint of Skeleton Data of User Data		(UserData.SkeletonData.Joint)
- Joint Type								(JointType)
- Frame Overload Utils						(FrameOverloadUtils)